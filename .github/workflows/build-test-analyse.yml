name: Build, Test & Analyse

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build-and-test-java-app:
    name: Build & Test Java App
    uses: DNAstack/dnastack-development-tools/.github/workflows/build-test-java-app.yml@6d4a5b5a58ed0494fca2d60edc1c9b98eac033a9
    with:
      java-version: 11
    secrets:
      pat-with-read-packages-permission: ${{ secrets.AUTH_TOKEN }}

  sast:
    name: SAST (SonarQube)
    uses: DNAstack/dnastack-development-tools/.github/workflows/sast.yml@6d4a5b5a58ed0494fca2d60edc1c9b98eac033a9
    with:
      with-frontend: false
      java-version: 11
    secrets:
      pat-with-read-packages-permission: ${{ secrets.AUTH_TOKEN }}
      sonar-token: ${{ secrets.SONAR_TOKEN }}
      sonar-host-url: ${{ secrets.SONAR_HOST_URL }}

  sca:
    name: SCA (Trivy)
    uses: DNAstack/dnastack-development-tools/.github/workflows/sca.yml@6d4a5b5a58ed0494fca2d60edc1c9b98eac033a9
    with:
      java-version: 11
    secrets:
      pat-with-read-packages-permission: ${{ secrets.AUTH_TOKEN }}

  secrets-detection:
    name: Secrets Detection (Gitleaks)
    uses: DNAstack/dnastack-development-tools/.github/workflows/secrets-detection.yml@6d4a5b5a58ed0494fca2d60edc1c9b98eac033a9
    secrets:
      gitleaks-license: ${{ secrets.GITLEAKS_LICENSE }}
